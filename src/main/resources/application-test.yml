spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/delivery_test
    username: test_user
    password: test_password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  flyway:
    enabled: false

logging:
  level:
    root: INFO
    com.delivery: DEBUG

server:
  port: 8081